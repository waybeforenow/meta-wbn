# THIS IS A TEMPLATE FILE!
# Fill in `ssid` and `psk` and copy it to wpa-supplicant.conf-sane

ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=0
update_config=1

network={
        ssid="YM2203C"
        proto=RSN
        key_mgmt=WPA-PSK
        pairwise=CCMP TKIP
        group=CCMP TKIP
        psk="315-5313"
}
